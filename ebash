#!/bin/bash
set -euo pipefail

# Версия скрипта
VERSION="1.3.1"

# Проверка переменных окружения
if [ -z "${TELEGRAM_BOT_TOKEN:-}" ] || [ -z "${TELEGRAM_CHAT_ID:-}" ]; then
  echo "Ошибка: задайте переменные TELEGRAM_BOT_TOKEN и TELEGRAM_CHAT_ID!" >&2
  echo "Использование: export TELEGRAM_BOT_TOKEN='токен'" >&2
  echo "           export TELEGRAM_CHAT_ID='идентификатор'" >&2
  exit 1
fi

# Проверка доступности cURL
if ! command -v curl &> /dev/null; then
  echo "Ошибка: curl не установлен!" >&2
  exit 1
fi

# Проверка доступности Telegram API
check_telegram_api() {
  if ! curl -s -f "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/getMe" > /dev/null; then
    echo "Ошибка: не удалось подключиться к Telegram API!" >&2
    echo "Проверьте токен бота и доступность сети" >&2
    return 1
  fi
  return 0
}

# Аргументы командной строки
if [ $# -eq 0 ]; then
  echo "Telegram Script Notifier v$VERSION"
  echo "Использование: $0 <script.sh> [аргументы...]" >&2
  echo "               $0 --version" >&2
  exit 1
fi

# Проверка версии
if [ "$1" == "--version" ]; then
  echo "ebash v$VERSION"
  exit 0
fi

# Временный файл лога
LOG_FILE=$(mktemp)
trap 'rm -f "$LOG_FILE"' EXIT

# Функция отправки в Telegram
send_telegram() {
  local message="$1"
  # Экранирование только обратных кавычек
  message="${message//\`/\\\`}"
  
  # Попытка отправки с игнорированием ошибок
  curl -s -X POST "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendMessage" \
    -d "chat_id=${TELEGRAM_CHAT_ID}" \
    -d "text=$message" \
    -d "parse_mode=Markdown" >/dev/null || true
}

# Получение имени команды
COMMAND_NAME=$(basename "$1")
START_MESSAGE="🚀 *Запущен скрипт:* \`$COMMAND_NAME\`
🖥 *Хост:* \`$(hostname)\`
📂 *Рабочий каталог:* \`$(pwd)\`
🕒 *Время запуска:* $(date +"%Y-%m-%d %H:%M:%S %Z")"

send_telegram "$START_MESSAGE"

# Обработка прерывания (Ctrl+C)
interrupted() {
  echo -e "\n[ebash] Прервано пользователем (Ctrl+C)"
  send_telegram "⏹ *Скрипт прерван:* \`$COMMAND_NAME\`
🖥 *Хост:* \`$(hostname)\`
🕒 *Время работы:* $((SECONDS)) сек"
  exit 130
}
trap interrupted SIGINT

# Запускаем скрипт с перехватом вывода
start_time=$(date +%s)
set +e
"$@" 2>&1 | tee "$LOG_FILE"
exit_code=${PIPESTATUS[0]}
set -e
end_time=$(date +%s)
duration=$((end_time - start_time))

# Форматируем длительность
formatted_duration=$(printf "%02d:%02d:%02d" $((duration/3600)) $(((duration%3600)/60)) $((duration%60)))

# Формируем статус
if [ $exit_code -eq 0 ]; then
  STATUS_EMOJI="✅"
  STATUS_TEXT="УСПЕШНО"
elif [ $exit_code -eq 130 ]; then
  STATUS_EMOJI="⏹"
  STATUS_TEXT="ПРЕРВАНО ПОЛЬЗОВАТЕЛЕМ"
else
  STATUS_EMOJI="❌"
  STATUS_TEXT="С ОШИБКОЙ"
fi

# Основное сообщение
MAIN_MESSAGE="$STATUS_EMOJI *Скрипт завершен:* \`$COMMAND_NAME\`
⌛ *Длительность:* $formatted_duration
📋 *Статус:* $STATUS_TEXT
🕒 *Время завершения:* $(date +"%Y-%m-%d %H:%M:%S %Z")
🔢 *Код выхода:* $exit_code"

# Получаем логи ДО точки ошибки
LOG_CONTENT=""
if [ $exit_code -ne 0 ]; then
  # Для ошибок: берем логи до последней ошибки
  LOG_CONTENT=$(grep -B 10 -i "error\|fail\|exception\|undefined\|not found\|no such file" "$LOG_FILE" 2>/dev/null | tail -n 20 || head -n 20 "$LOG_FILE")
else
  # Для успеха: последние 10 строк
  LOG_CONTENT=$(tail -n 10 "$LOG_FILE" 2>/dev/null || echo "Логи недоступны")
fi

# Формируем полное сообщение
FULL_MESSAGE="$MAIN_MESSAGE
📝 *Логи:*
\`\`\`
$LOG_CONTENT
\`\`\`"

# Отправляем сообщение
send_telegram "$FULL_MESSAGE" || {
  # Если не удалось, отправляем только основное сообщение
  send_telegram "$MAIN_MESSAGE"
}

exit $exit_code